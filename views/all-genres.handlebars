<section>
    <p> This is all-genres.handlebars!</p>
    <ul>
        {{#each genres as |genre|}}
        <li>Genre name: {{genre.genre_name}}</li>
        <li>Genre id number: {{genre.genre_id}}</li>
        {{/each}}
</section>