<section>
    <p class="card-title">Delete this Review?</p>
    <ul>
        <li class="card-title">Review title: {{review.review_title}}</li>
        <li>Review text: {{review.review_text}}</li>
        <li id="user">Review user name: {{review.user.username}}</li>
        <li id="gamename">Review game name: {{review.game.game_name}}</li>
        <li>Date of review: {{review.review_date}}</li>
    </ul>
<p> Are you sure that you want to delete this review? </p>
<form id="delete-review-form">
   <input type="hidden" name="review-id" value="{{review.id}}" />

   <button type="button" class="btn-primary btn" id="delete-btn">Delete</button>
</form>
</section>

<script src="/js/deleteReview.js"></script>